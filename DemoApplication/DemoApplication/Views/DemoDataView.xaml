<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="DemoApplication.Views.DemoDataView">
  <ContentView.Content>
    <StackLayout>
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Text="Property1:" />
        <Label Grid.Row="0" Grid.Column="1" Text="{Binding Path=Data[Property1]}" />

        <Label Grid.Row="1" Grid.Column="0" Text="Property2:" />
        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Path=Data[Property2]}" />
      </Grid>

      <Button Text="Navigate :D" Command="{Binding Path=NavigateCommand}" />
      <Button Text="Manipulate Data" Command="{Binding Path=ManipulateDataCommand}" />
    </StackLayout>
  </ContentView.Content>
</ContentView>
